
        /*let hash = "#";
            console.log(hash);
            console.log(hash + hash);
            console.log(hash + hash + hash);
            console.log(hash + hash + hash + hash);
            console.log(hash + hash + hash + hash + hash);
            console.log(hash + hash + hash + hash + hash + hash);
            console.log(hash + hash + hash + hash + hash + hash + hash);
        */

            let hashtag = "#";

            console.log(hashtag);

            while (true) {
                hashtag = hashtag + "#";
                console.log(hashtag);
                if (hashtag == "############" ) break;
            }
